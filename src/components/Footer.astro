---
import { ArrowUpRight, GitBranch } from '@lucide/astro';
---

<footer class="site-footer">
  <div class="page-shell footer-inner">
    <div>
      <a class="footer-brand" href="/">
        <img src="/assets/icon.png" alt="" width="28" height="28" />
        <span>ACP Pro</span>
      </a>
      <p>Unified chat for AI coding agents in VS Code.</p>
    </div>
    <div class="footer-links">
      <a href="https://marketplace.visualstudio.com/items?itemName=duclvz.acp-pro">
        Marketplace
        <ArrowUpRight size={14} aria-hidden="true" />
      </a>
      <a href="https://open-vsx.org/extension/duclvz/acp-pro">
        Open VSX
        <ArrowUpRight size={14} aria-hidden="true" />
      </a>
      <a href="https://github.com/acp-pro/acp-pro.github.io">
        <GitBranch size={14} aria-hidden="true" />
        GitHub
      </a>
      <a href="https://duclvz.gumroad.com/l/acp-pro">
        Pro license
        <ArrowUpRight size={14} aria-hidden="true" />
      </a>
      <a href="https://github.com/duclvz">
        @duclvz
        <ArrowUpRight size={14} aria-hidden="true" />
      </a>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    border-top: 1px solid var(--line);
    padding: 1.75rem 0;
    background: var(--bg);
  }

  .footer-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 24px;
  }

  .footer-brand,
  .footer-links,
  .footer-links a {
    display: flex;
    align-items: center;
  }

  .footer-brand {
    gap: 10px;
    font-size: 0.95rem;
    font-weight: 700;
  }

  .footer-brand:focus-visible {
    outline-offset: 4px;
    border-radius: 6px;
  }

  .footer-brand img {
    border-radius: var(--radius-sm);
    border: 1px solid var(--line-strong);
  }

  .site-footer p {
    margin: 0.45rem 0 0;
    color: var(--muted);
    font-size: 0.875rem;
    line-height: 1.5;
  }

  .footer-links {
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: 1rem 1.15rem;
    color: var(--muted);
    font-family: var(--font-mono);
    font-size: 0.75rem;
    font-weight: 500;
  }

  .footer-links a {
    display: inline-flex;
    align-items: center;
    gap: 4px;
    padding: 0.25rem 0.05rem;
    border-radius: 4px;
    transition: color var(--dur-state) var(--ease-feedback);
  }

  .footer-links a:hover {
    color: var(--ink);
  }

  .footer-links a:focus-visible {
    outline-offset: 4px;
  }

  @media (max-width: 720px) {
    .footer-inner {
      align-items: flex-start;
      flex-direction: column;
    }

    .footer-links {
      justify-content: flex-start;
    }
  }
</style>
