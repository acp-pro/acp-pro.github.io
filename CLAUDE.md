# ACP Pro Homepage

## Spec-Driven Development

This project uses openspec for spec-driven development.

- Run `/spx` for new features or changes
- Run `/spx --fix <name>` for bug fixes
- Specs live in `openspec/specs/`
- Work-in-progress lives in `openspec/changes/`

See `openspec/PROJECT.md` for capabilities and constraints.
